on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    container: node:18
    steps:
      - run: node --version
      - run: npm --version
      - uses: actions/checkout@v3
      - run: cd scripts
      - run: npm install
      - run: node script.js