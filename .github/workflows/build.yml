name: Run Script

on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    container: node:18
    steps:
      - run: node --version
      - run: npm --version
      - uses: actions/checkout@v3
      - name: Run scripts
        working-directory: ./scripts
        run: |
          npm install
          node script.js
